import os

PROJECT_ROOT = os.path.dirname(os.path.dirname(__file__))

# ============= 設定 ==============

INPUT_DIR = os.path.join(PROJECT_ROOT, "input")  # 入力ディレクトリを設定
OUTPUT_DIR = os.path.join(PROJECT_ROOT, "output")  # 出力ディレクトリを設定
TMP_DIR = os.path.join(PROJECT_ROOT, "tmp")  # 一時ディレクトリを設定

# ディレクトリが存在しない場合は作成
os.makedirs(INPUT_DIR, exist_ok=True)
os.makedirs(OUTPUT_DIR, exist_ok=True)
os.makedirs(TMP_DIR, exist_ok=True)

# 変更する速度
SPEED = 1.25
